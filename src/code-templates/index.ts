import { ApiRecord, PC3 } from "../types.js";

export const jsTemp = (pccc: PC3, redisEntries: ApiRecord[]) => {
    // TODO
    /*
    return `const pc = new PektinClient({});
  const endpoint="${endpoint}";
  const res = await fetch(endpoint + "/set", {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({
          token,
          records: 
                  ${JSON.stringify(
                    redisEntries,
                    null,
                    "    "
                  )}
      })
  }).catch(e => {
      console.log(e);
  });
  console.log(res);`;*/
};
